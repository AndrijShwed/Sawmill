@page
@model LoginModel

@{
    ViewData["Title"] = "Увійти";
}

<!DOCTYPE html>

<html>
<head>
    <style>
        .block {
            width: 1300px;
            height: 600px;
            background-image: url(/images/Зруб_3.jpg);
            background-size: cover;
            background-repeat: no-repeat;
            padding: 5px;
            padding-right: 5px;
            border: solid 5px black;
            float: left;
        }

    </style>

</head>
<body>
    <div class="block">
<h1 style="color:white">@ViewData["Title"]</h1>
<div class="row">
    <div class="col-md-4">
        <section>
            <form id="account" method="post">
                <h2 style="color:white">Використовуйте свій акаунт для входу</h2>
                <hr />
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                    <label asp-for="Input.Email" class="form-label">Email</label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                    <label asp-for="Input.Password" class="form-label">Пароль</label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div style=" width:220px; height:25px; background-color:azure; border-radius:0.5em">
                <div class="checkbox mb-3" style="text-align:center">
                    <label asp-for="Input.RememberMe" class="form-label">
                        <input class="form-check-input" asp-for="Input.RememberMe" />
                        @Html.DisplayNameFor(m => m.Input.RememberMe)
                    </label>
                </div>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary" style="margin-top:10px">Увійти</button>
                </div>
                <div style="width:220px; height:150px; background-color:azure; margin-top:10px; border-radius:0.5em">
                    <p style="text-align:center">
                        <a id="forgot-password" asp-page="./ForgotPassword">Забули свій пароль?</a>
                    </p>
                    <p style="text-align:center">
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Зареєструватись</a>
                    </p>
                    <p style="text-align:center">
                        <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Надіслати повторний лист на ел.пошту для підтвердження</a>
                    </p>
                </div>
            </form>
        </section>
    </div>
    @* <div class="col-md-6 col-md-offset-2">
        <section>
            <h3>Use another service to log in.</h3>
            <hr />
            @{
                if ((Model.ExternalLogins?.Count ?? 0) == 0)
                {
                    <div>
                        <p>
                            There are no external authentication services configured. See this <a href="https://go.microsoft.com/fwlink/?LinkID=532715">article
                            about setting up this ASP.NET application to support logging in via external services</a>.
                        </p>
                    </div>
                }
                else
                {
                    <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                        <div>
                            <p>
                                @foreach (var provider in Model.ExternalLogins!)
                                {
                                    <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                }
                            </p>
                        </div>
                    </form>
                }
            }
        </section>
    </div> *@
</div>
</div>
</body>
</html>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
